#constante miConstante 0;
tipo entero vector[miConstante];

entero resta(entero x){
    devuelve 2 + 3;
}

vacio suma (){
    devuelve;
}

vacio principal () {
    entero a;
    vector v;
    a = resta(miConstante);
    escribeEnt(3);
    devuelve;
}