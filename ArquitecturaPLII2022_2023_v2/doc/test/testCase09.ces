/* 

// Operadores logicos &&
*/

vacio principal () {

    entero a;

    a = 2 && 0;

	escribe("a = 0?: ");
	escribeEnt(a);
	escribe();

    a = 2 && 1;

	escribe("a = 1?: ");
	escribeEnt(a);
	escribe();

    devuelve;

}