/* 

// Operadores logicos &&
*/

vacio principal () {

    entero a;
    entero y = 7;
    entero z = 0;
    
    si (z == 0 && !y == z && (!(!y) && !z)) {
        escribe ("correcto");
    }sino {
        escribe("error");
    }

    a = 2 && 0;

	escribe("a = 0?: ");
	escribeEnt(a);
	escribe();

    a = 2 && 1;

	escribe("a = 1?: ");
	escribeEnt(a);
	escribe();

    devuelve;

}