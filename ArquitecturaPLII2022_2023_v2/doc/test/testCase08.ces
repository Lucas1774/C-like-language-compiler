/* operadores lógicos
*/

vacio principal () {

    entero a;
    entero b;
    entero c;

    a=5;
	escribe("a = 5?: ");
	escribeEnt(a);
	escribe();

    b=3;
	escribe("b = 3?: ");
	escribeEnt(b);
	escribe();

    c = a < b;
	escribe("c = 0?: ");
	escribeEnt(c);
	escribe();

    c = !(a == b);
	escribe("c = 1?: ");
	escribeEnt(c);
	escribe();

    c= !(a < b);
	escribe("c = 1?: ");
	escribeEnt(c);
	escribe();

    c = a == b;
	escribe("c = 0?: ");
	escribeEnt(c);
	escribe();


    devuelve;

}