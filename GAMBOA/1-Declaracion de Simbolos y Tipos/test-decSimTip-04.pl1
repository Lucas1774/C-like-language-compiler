# Ejemplo declaracion de simbolos y tipos

programa Ejemplo:
	constantes
		MAX = 5;
		#  MAX = falso;  # Error el identificar MAX ya esta declarado en la tabla de simbolos

	tipos
		Mivector = vector[1..5] de entero;
		# MivectorMAX = vector[1..MIN] de entero;  # Error el valor del rango MIN no esta declarado como constante simbolica en la tabla de simbolos
		# MAX = vector[1..100] de booleano; # Error el identificador MAX ya esta declarado en la tabla de simbolos.
	
	variables
		i : entero;
		x : booleano;
		v1 : Mivector;
		# MAX : booleano; # Error el identificar MAX ya esta declarado en la tabla de simbolos del mismo ambito
		# Mivector : booleano; # Error el identificar Mivector ya esta declarado en la tabla de tipos abiertas
		# v2 : Tuvector; # Error el tipo Tuvector no esta declarado en la tabla de tipos
		
	subprogramas	
		funcion EsMenorQueDos (a:entero): booleano:
		# funcion x (a:entero): booleano:  # Error el identificador de la función X ya esta declarado en la tabla de simbolos
			tipos
				# Mivector = vector[1..100] de booleano; # Error el identificaor Mivector ya esta declarado en la tabla de tipos
				# Tuvector = vector[1..MAX] de booleano; # no debe dar error  el valor del rango MAX es una constante simbolica declarada en la pila de tabla de simbolos abiertas
				# Tuvector = vector[1..A] de booleano;   # Error el valor del rango A no es una constante simbolica
				# Tuvector = vector[100..1] de booleano; # Error el valor del rango n1 es mayor que el valor del rango n2
			
			variables
				dos : entero;
				result : entero;
				esMayor : booleano;
				# DOS : booleano;  		# Error el identificar dos ya esta declarado en la tabla de simbolos del mismo ambito 
				# Mivector : booleano; 	# Error el identificar Mivector ya esta declarado en la tabla de tipos abiertas
				# v1 : Mivector;		# no debe dar error dado que el tipo Mivector esta declarado en la tabla de tipos abiertos
				# v2 : EsMenorQueDos;   # Error el identificar EsMenorQueDos no es de tipo vector
				
			subprogramas
				procedimiento Imprime (numero:entero):
				# procedimiento Mivector (numero:entero):  # Error el identificador del procedmiento Mivector ya esta declarado en la tabla de tipos abiertos
					constantes
					# MAX = 100;  # no debe dar error dado que el idenficador MAX esta declarado en otro ambito distinto
					# NUMERO = 100; # Error el idenficador NUMERO ya esta declarado en la tabla de simbolos del ambito del subprograma como un parametro formal
					# ESMENORQUEDOS = 1; # Error el idenficador ESMENORQUEDOS ya esta declarado en la tabla de tipos abiertos
					
					tipos
					# ESMENORQUEDOS = vector[1..100] de booleano; # Error el identificaor ESMENORQUEDOS ya esta declarado en la tabla de tipos abiertos
				
				comienzo
					
					escribir("Procedimiento de ambito de nivel 2");

				fin;
		
		comienzo
			
			escribir("Funcion de ambito de nivel 1");
					
		fin;
		

comienzo
	escribir("hola");
	escribir();			
	escribir("mundo");
fin.
