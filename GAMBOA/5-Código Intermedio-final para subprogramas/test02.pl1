# Ejemplo generacion codigo intermedio y final invocando un subprograma

programa test:
  variables z : entero;
  subprogramas
  procedimiento incrementar (VAR x:entero):
  	variables a: entero;
 	comienzo
	    a = x + 1;
		x = a;
 	fin; 	
  
 comienzo
    z = 999;
	incrementar(z);
    escribir("z(1000)= ");
	escribir(z);
	escribir();
 fin.